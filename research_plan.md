# Stochastic Critical Bands in Falkner–Skan Boundary Layers Under Colored Pressure‑Gradient Noise

## 1. 背景与动机

经典 Falkner–Skan 方程用于描述带有流向压力梯度的二维层流边界层流动，其确定性解预测了一条临界值 \(\beta_c \approx -0.1988\)——当压力梯度参数 \(\beta\) 小于该值时，壁面剪切 \(f''(0)\) 为零并出现逆流现象【65357245362651†screenshot】。然而工程和自然系统中流动常常受到随机扰动，如不确定的压力梯度或外部激励，这促使我们将 \(\beta\) 视为随机过程而非固定常数。现有研究多关注 Falkner–Skan 方程的确定性求解、机器学习求解及随机涡法模拟，但针对**有色噪声驱动下的壁面临界分离的概率化描述**尚缺乏系统探讨。

## 2. 研究目标

本课题旨在提出一种低成本数值框架，通过构建沿壁面方向相关的 Ornstein–Uhlenbeck 随机场 \(\beta(x)\)，将确定性的分离临界点转化为**随机临界带**，并研究噪声强度 \(\sigma\) 与相关长度 \(\ell_c\) 对分离概率和临界带宽度的影响。核心问题包括：

1. **随机临界带的定义与构建**：对于给定平均压力梯度 \(\bar\beta\) 和噪声特征 \((\sigma, \ell_c)\)，如何利用局部 Falkner–Skan 解映射 \(\alpha(x) = f''(0; \beta(x))\) 构建临界事件 \(\min_x \alpha(x) \le \epsilon\) 的概率，进而确定 5%、50%、95% 分位数对应的\(\beta\) 区间？
2. **不确定度传播方法的选择**：比较非侵入式 Monte Carlo、广义多项式混沌 (gPC) 和代理模型（如前馈神经网络）的效率和精度，确定适合于边值问题的不确定度传播策略【65357245362651†screenshot】。
3. **相关长度的物理意义**：分析 OU 有色噪声的相关长度 \(\ell_c\) 如何影响沿壁面持续不利压梯区间的出现，以及其对分离概率和临界带宽度的调制作用。
4. **敏感性分析与尺度律**：利用 Sobol 指数或 gPC 系数分解输出方差，确定平均压力梯度、噪声强度和相关长度对随机临界带宽度的相对贡献，并拟合 \(W(\sigma, \ell_c)\) 的标度律。

## 3. 方法与步骤

1. **确定性基线与灵敏度**：使用 shooting 方法或 SciPy `solve_bvp` 求解 Falkner–Skan 方程，得到壁面剪切 \(\alpha(\beta) = f''(0; \beta)\) 与压力梯度之间的映射，并通过对 \(\beta\) 的线性化方程求解 \(\partial \alpha/\partial \beta\) 获取灵敏度曲线。此基线验证模型并提供近临界响应斜率。
2. **OU 随机场生成**：构造沿无量纲壁面坐标 \(x \in [0,1]\) 的 Ornstein–Uhlenbeck 随机场 \(\beta(x)\)，其均值为 \(\bar\beta\)，方差 \(\sigma^2\)，相关长度 \(\ell_c\)。数值上可采用 Euler–Maruyama 离散化生成多条样本路径。
3. **局部解与事件定义**：对每个离散 \(x_i\)，用插值查表或快速求解器计算 \(\alpha(x_i) = f''(0; \beta(x_i))\)。定义 near–separation 事件为 \(\alpha(x) \le \epsilon\) 且持续区间长度大于 \(L_{sep}\)。
4. **不确定度传播与统计**：在蒙特卡罗框架下生成大量 OU 路径，统计路径级别的\(\alpha_{\min} = \min_x \alpha(x)\) 分布、分离概率以及 5/50/95 分位数，并研究不同 \((\bar\beta, \sigma, \ell_c)\) 组合对结果的影响。
5. **敏感性分析与 gPC**：实现非侵入式 gPC 展开，将随机参数映射到多项式基底，计算系数并评估均值、方差及 Sobol 指数；与蒙特卡罗结果比较计算效率。
6. **结果可视化与尺度律拟合**：绘制确定性 \(\alpha(\beta)\) 曲线、随机临界带宽度 \(W\) 随 \(\sigma, \ell_c\) 的关系曲线和分离概率随平均 \(\bar\beta\) 的热力图。拟合功率律或指数关系分析数据趋势。

## 4. 数据与代码

本研究的主要文件已包含在仓库中：

- `falkner_skan_sim.py`：实现 Falkner–Skan 边值问题求解、OU 随机场生成、Monte Carlo 仿真及数据导出。
- `falkner_skan_ou_samples.csv`：包含随机样本路径的最小壁面剪切值和其他统计。（文件较大，可选）
- `falkner_skan_ou_summary.csv`：整理了不同 \((\bar\beta, \sigma, \ell_c)\) 组合下分离概率、平均最小剪切等指标的统计值。
- `full_report.md`：完整研究报告，包含背景综述、方法、初步结果、讨论及结论。

## 5. 可行性与创新性

1. **现有文献空缺**：已有工作主要关注 Falkner–Skan 方程的确定性求解、采用机器学习或混合算法求解器、以及随机涡方法模拟经典边界层【65357245362651†screenshot】。对于**沿壁面相关的有色噪声如何将分离临界点转化为概率化临界带**，目前尚未见专门研究。这表明本课题在将随机过程与经典边界层临界理论结合方面具有创新性。
2. **非显然结果**：虽然\(\beta\) 与 \(f'''(0)\) 之间有直接线性关系，\(f''(0)\) 作为整个边值问题的 shooting 参数，其对 \(\beta\) 的非线性依赖、尤其在临界区的放大效应，并不能简单通过公式推导获得。更进一步，路径级别事件依赖于随机场的相关结构，其分离概率与相关长度的关系无法从局部灵敏度解析得到。通过数值模拟和不确定度传播来量化这些现象，属于需要探索的新问题。
3. **潜在应用**：该研究框架不仅可以用于 Falkner–Skan 模型，也可以推广到其他压梯层流模型及湍流流动的初始不确定度传播，为机翼失速预测、火箭喷管侧向载荷分析等提供理论依据。

## 6. 操作说明

1. 克隆或访问 `falknerskan` 仓库，确保安装 Python 环境和必要库（如 `numpy`, `scipy`, `pandas`）。
2. 运行脚本：
   ```bash
   python3 falkner_skan_sim.py \
       --num_paths 1000 \
       --beta_mean -0.165 \
       --sigma 0.03 \
       --corr_len 0.1 \
       --eps 0.02 \
       --output samples.csv
   ```
   该命令生成 1000 条 OU 路径并计算最小壁面剪切，输出到 `samples.csv`，并将统计信息打印到标准输出。
3. 使用 `falkner_skan_ou_summary.csv` 查看不同参数组合的分离概率与平均值，用于绘图或进一步分析。
4. 阅读 `full_report.md` 获取理论背景、初步结果与讨论，结合本计划开展后续研究或扩展。

## 7. 预期成果与下一步工作

本计划预计补充如下内容：

1. **生成更大规模 OU 样本数据**，验证初步结论的稳定性，并绘制分离概率热图与随机临界带宽度曲线。
2. **完成 gPC 实现**，比较其与蒙特卡罗方法在效率与精度上的差异。
3. **开展 Sobol 敏感性分析**，计算输入参数对输出方差的贡献，并讨论物理意义。
4. **撰写论文草稿**，包括摘要、引言、方法、结果和讨论，形成可投稿的完整文稿。

完成上述工作后，可以进一步考虑在 Falkner–Skan 模型中加入质量传输、壁面运动等物理机制，扩展到非相似边界层方程，或将该不确定度传播框架推广到高保真 CFD。